
pipeline {
  agent any {
    docker { 
    image 'node:16-alpine'
    args '-u root'
    alwaysPull false
    reusenode true
     }
  }
  stages {
    stage('Build') {
      steps {
        sh 'npm-install'
      }
    }
  }
}